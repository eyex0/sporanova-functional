# NOVA Training — Python Dependencies
# Install: pip install -r requirements.txt
# Tested with: Python 3.11+, CUDA 12.1+, Ubuntu 22.04

# ── Core Framework ────────────────────────────────────────────────
torch>=2.4.0
transformers>=4.46.0
peft>=0.14.0
trl>=0.12.0
datasets>=3.2.0
accelerate>=1.2.0

# ── Quantization (QLoRA) ─────────────────────────────────────────
bitsandbytes>=0.45.0

# ── DeepSpeed (Full Fine-Tuning) ─────────────────────────────────
deepspeed>=0.15.0

# ── Tokenizer ─────────────────────────────────────────────────────
tokenizers>=0.21.0
sentencepiece>=0.2.0

# ── Evaluation ────────────────────────────────────────────────────
rouge-score>=0.1.2
nltk>=3.9.0
sacrebleu>=2.5.0

# ── Utilities ─────────────────────────────────────────────────────
pyyaml>=6.0.2
jsonlines>=5.0.0
tqdm>=4.67.0
numpy>=1.26.0
wandb>=0.19.0

# ── Optional: vLLM for fast inference during evaluation ──────────
# Uncomment if using vLLM for evaluation:
# vllm>=0.7.0
